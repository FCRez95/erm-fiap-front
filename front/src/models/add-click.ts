export interface AddClickModel {
	idSite: number | null,
	siteDomain: string | null,
	thumbnail: string | null,
	title: string | null,
	timeStamp: string | null,
	platform: string | null,
	idCampaing: number | null,
	nameCampaing: string | null,
	token: string | null
}